export interface ReceiptInput { receiptNo: string; date: string; studentName: string; studentId: string; courseName: string; paymentMode: string; amount: number; totalFee: number; previouslyPaid: number; remainingBalance: number }
const money = (value: number) => value.toLocaleString("en-IN", { minimumFractionDigits: 2 });
const escape = (value: string) => value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character] ?? character);
export function buildReceiptHtml(data: ReceiptInput): string {
  const lines = [["Receipt No", data.receiptNo], ["Date", data.date], ["Student", data.studentName], ["Student ID", data.studentId], ["Course", data.courseName], ["Payment mode", data.paymentMode], ["Total fee", `₹${money(data.totalFee)}`], ["Previously paid", `₹${money(data.previouslyPaid)}`], ["Current payment", `₹${money(data.amount)}`], ["Remaining balance", `₹${money(data.remainingBalance)}`]];
  return `<!doctype html><html><head><title>Fee Receipt</title><style>body{font-family:Arial;color:#334155;padding:30px}.receipt{max-width:760px;margin:auto;border:1px solid #ddd;padding:36px}h1{color:#6C3CE9}.row{display:flex;justify-content:space-between;padding:10px;border-bottom:1px solid #eee}.row:nth-last-child(2){background:#6C3CE9;color:white;font-weight:bold}@media print{button{display:none}}</style></head><body><div class="receipt"><h1>VIVEXA INSTITUTE OF TECHNOLOGY</h1><h2>Fee Receipt</h2>${lines.map(([key, value]) => `<div class="row"><span>${escape(String(key))}</span><strong>${escape(String(value))}</strong></div>`).join("")}<p style="text-align:center;font-size:12px">Computer-generated receipt</p><button onclick="window.print()">Print / Save PDF</button></div></body></html>`;
}
